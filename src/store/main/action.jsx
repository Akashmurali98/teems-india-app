// import "../main/Reducers.jsx"
import RolesApi from "../../Service/Roles";
import { loaderOpening, loaderClosing } from "./Reducers";

export const Loader = () => (dispatch) => {
  // dispatch(loaderOpening());
  
  // dispatch(loaderClosing());
};
